# Definition of CIV

_Sidecar for image `Definition of CIV.png`. OCR text below; refer to the PNG for the visual._

## OCR text

```
“Cumulative Investment Value” (CIV) is just the time-series you get by
assuming you invested 1.0 unit at the start and then reinvested all

gains each period. Concretely, if your daily returns are 71, 79,..., then

Because CIVp = 1, every subsequent value CIV; includes the
original unit plus all compounded gains up to day f. If you actually
invested $100, you'd scale the series by 100 afterward; the CIV itself is

the “per-unit-invested” growth curve.

So in other words, it is the scaled Current
Investment Value series?

Exactly. CIV is the “current investment value” at each date, scaled so

that its value on the first date is 1.0. In other words,

Current Portfolio Value;

IV, —
CIV Initial Portfolio Valueo

?

so it’s the current value series normalized to a per-unit investment.

Opinion (high confidence): calling it a “scaled current investment
value” is accurate, but in back-testing parlance “cumulative
investment value” is more commonly used to emphasize the

compounding.
```
