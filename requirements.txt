# Canonical install is the editable package: `pip install -e ".[dev]"`
# (it also installs the portfolio-analyzer* console scripts). This file is a
# convenience mirror of the runtime deps in pyproject.toml — keep it in sync.
pandas==2.2.3
numpy==1.26.3
requests==2.32.3
toml==0.10.2
urllib3==2.1.0
beautifulsoup4>=4.12.3
statsmodels==0.14.4
matplotlib==3.8.2
# matplotlib's font-pattern parser uses pyparsing APIs deprecated in >=3.2,
# whose warnings our pytest config escalates to errors; pin the last 3.1.x.
pyparsing==3.1.4
tabulate
